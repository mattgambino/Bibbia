# ROADMAP operativa

Regole di esecuzione:
- **Un task per sessione**, nell'ordine. A fine task: `npm run valida` verde (da F0.3 in poi), riepilogo breve, **stop**.
- Non iniziare il task o la fase successiva senza ok esplicito dell'utente.
- I task marcati **⏸ REVISIONE** producono bozze in `bootstrap/` e si fermano lì: lo spostamento in `public/data/` avviene solo dopo revisione umana. Non sono ⏸ REVISIONE, e si scrivono quindi **direttamente in `public/data/`**, i contenuti approvati dall'utente pezzo per pezzo durante la sessione (es. la traduzione letterale, approvata pericope per pericope): lì l'approvazione è già la revisione. Vale la regola 3 di CLAUDE.md; il discrimine è se l'utente ha visto e approvato il contenuto, non chi ha battuto il testo.
- Aggiornare lo stato dei task in questo file (`[ ]` → `[x]`) man mano.
- **Ogni task di curation estende `lexicon_it.json`** (glosse italiane per lemma) con i lemmi del range appena curato: l'italiano cresce capitolo per capitolo insieme al resto, non in un blocco finale. Vale per F2.1 (già fatto → recuperato in F2.5), F2.3, F3.4 e ogni curation successiva.

---

## Fase 0 — Fondamenta

- [x] **F0.1 Scaffold.** Vite + React + TypeScript; struttura cartelle come SCHEMI-DATI §3; dipendenze minime (react, react-dom, zod; dev: typescript, vite, tsx). Script npm: `dev`, `build`, `preview`, `valida` (placeholder). Leaflet e D3 NON ancora: si installano nella fase in cui servono.
  *Fatto quando:* `npm run dev` mostra la shell vuota dell'app e la struttura cartelle esiste (con `.gitkeep` dove serve), `.gitignore` copre `scripts/sources/` e `node_modules`.
- [x] **F0.2 Schemi Zod.** In `src/tipi/`: tutti gli schemi di SCHEMI-DATI §2, inclusi i tipi condivisi (`Fonte`, `Range`, enum confidenza, target polimorfo delle note) e i tipi derivati con `z.infer`.
  *Fatto quando:* compilazione pulita e tipi esportati; nessuna definizione di tipo duplicata a mano.
- [x] **F0.3 Validatore.** `scripts/valida.ts`: valida ogni JSON presente in `public/data/` e `bootstrap/` (default; altre directory passabili come argomento) contro gli schemi + controlli incrociati (riferimenti esistenti; reciprocità relazioni familiari; copertura contigua delle pericopi sul range curato; coerenza fonti ↔ `da_verificare`; chiavi traduzioni risolvibili su id TM; `commentatore` e `sefaria_ref` valorizzati solo quando `tipo = "tradizione_ebraica"`; `da ≤ a` in `RangeAnni` e `RangeVersetti`, per i versetti stesso libro e confronto capitolo/versetto). Output leggibile: file, record, errore; exit code diverso da 0 in presenza di errori.
  *Fatto quando:* `npm run valida` gira su un mini-dataset di prova (valido e volutamente rotto, in `scripts/fixtures/`, solo placeholder dichiaratamente finti) con esiti corretti.
- [x] **F0.4 Loader + stub morfologia.** `src/dati/`: caricamento JSON con cache per libro e hook React; `src/lib/morfologia.ts` con la decodifica di una prima manciata di codici comuni e fallback "codice non decodificato: <sigla>".
  *Fatto quando:* l'app carica un `verses/gen.json` di prova e lo mostra in forma grezza.
  *Nota:* i `verses/gen.json` e `words/gen.json` ora in `public/data/` sono copie delle fixture dichiaratamente finte di F0.3 (il `meta` lo dichiara): verranno sovrascritti dall'import TAHOT in F1.1.

## Fase 1 — Import e vista lettura

- [x] **F1.1 Import TAHOT.** Leggere la documentazione del formato nel repo STEPBible-Data; `scripts/import-tahot.ts` genera `verses/` e `words/` per i 5 libri, con `meta` e attribuzione. Documentare nel codice il mapping colonne→campi deciso.
  *Fatto quando:* validazione verde sull'intero Pentateuco; conteggi versetti/parole plausibili riportati nel riepilogo.
- [x] **F1.2 Indice lemmi.** Estensione dell'import: `indices/lemmi.json` (chiave dStrong, occorrenze).
- [x] **F1.3 Import TIPNR + OpenBible.** ⏸ REVISIONE — `scripts/import-tipnr.ts` produce bozze `places.json` e `people.json` in `bootstrap/` (luoghi con candidati e `peso_openbible`; persone con relazioni e riferimenti; tutto `da_verificare: true`, `status` NON assegnato automaticamente oltre un default prudente).
  *Revisione della bozza (dopo F2.2), quattro correzioni allo script e rigenerazione:*
  1. **`nomi.translit` vuoti.** TIPNR e TAHOT disambiguano lo stesso nome con lettere diverse (`H3876G` vs `H3876` per Lot), così la ricerca esatta sull'indice dei lemmi falliva. Aggiunto il ripiego sul numero Strong nudo — valido solo per entità che hanno una forma ebraica propria, altrimenti «una moglie di Lot» (H3876J) verrebbe traslitterata "Lot". Vuoti da 30 a 23: restano 9 entità senza nome nel testo (per cui il vuoto è il dato giusto) e 14 il cui nome non compare nel Pentateuco con quella forma (Gerusalemme, Adullam, Abiezer/Iezer…).
  2. **`dati_narrativi` sempre null.** Le età non sono in TIPNR ma nel testo, e si leggono: nuovo estrattore che parte dalle parole già importate in `words/`, prende il valore dei numerali dalle glosse TBESH e il numero grammaticale dal codice morfologico TAHOT. Riconosce le due formule genealogiche vincolandole alla forma verbale — «generò» solo come wayyiqtol hifil וַיּוֹלֶד, «i giorni di» solo come plurale costrutto יְמֵי — ed è quel vincolo a tenere fuori i falsi positivi (Gen 7,13 «in quel giorno… tre», Gen 21,5 «quando gli nacque Isacco», le feste del settimo giorno in Levitico). L'attribuzione alla persona vuole numero Strong **e** versetto fra i suoi riferimenti TIPNR: è il secondo criterio a separare gli omonimi. Risultato: 22 persone, l'intera catena di Gen 5 e Gen 11 più Noè (950, Gen 9,29), Abramo, Isacco, Israele, Terach. Nessuna cifra scritta a memoria, nessun caso letto e non attribuito.
  3. **Naamah moglie di Noè.** Identificazione di Bereshit Rabbah 23,3 — contestata già lì: R. Abba bar Kahana la sostiene, i Rabbanan la respingono — che TIPNR codifica come parentela (marcandola però `(?)`), e che dentro `relazioni` sarebbe indistinguibile da un dato del racconto. Tolti i quattro archi (coniuge di Noè, madre di Sem/Cam/Iafet) tramite un elenco esplicito nello script, e scritta la nota `naamah-moglie-di-noe` (`tradizione_ebraica`, `speculative`, `sefaria_ref` verificato con l'API v3: versione CC-BY). Poiché un'identificazione extratestuale non è riconoscibile in automatico, lo script ora stampa anche i **legami sospetti** — coniugi o genitori che non condividono nessun versetto: 87 casi da controllare in revisione.
  4. **Convenzione degli omonimi.** Lo script dava lo slug nudo all'omonimo canonicamente più antico, cioè `lamech` al discendente di Caino e `lamech.gen5` al padre di Noè — il contrario di quel che un lettore si aspetta, e comunque una regola invisibile da fuori. Ora il disambiguatore lo prendono tutti (`lamech.gen4` / `lamech.gen5`), come già diceva SCHEMI-DATI §1: uno slug nudo garantisce che omonimi non ce ne siano. 89 persone e 12 luoghi rinominati; la curation di F2.1 è stata riportata sui nuovi id agganciandola al `tipnr_id`, e in `events.json` un solo riferimento andava riallineato (`havilah` → `havilah.gen2`).
  *Attenzione per il futuro (aggiornato dopo F1.3-fix e lo spostamento in `public/data/`):* i file curati ora vivono in `public/data/`, `import-tipnr.ts` continua a scrivere in `bootstrap/`, quindi rigenerare **non** sovrascrive più la curation. Resta però che la bozza rigenerata è un doppione dei record curati: `valida.ts` non lo tratta come errore — durante una revisione le due copie coesistono per forza — ma emette un avviso, perché a valle vince il file letto per primo e una bozza dimenticata metterebbe in ombra il record curato. Il flusso di una rigenerazione è quindi: `npx tsx scripts/import-tipnr.ts` (scrive le bozze pristine in `bootstrap/`), `npx tsx scripts/curation-override.ts estrai public/data bootstrap` (confronta le due directory e aggiorna `curation-override.json`, indicizzato per `tipnr_id`, che è stabile), poi si riportano a mano in `public/data/` le sole differenze volute e si svuota `bootstrap/`. Per riapplicare in blocco la curation su una bozza: `npx tsx scripts/curation-override.ts applica bootstrap` — ma il confronto col valore generato ha senso solo su una bozza appena rigenerata, su un file già patchato segnala divergenza su ogni campo.
- [x] **F1.4 Import Luzzi.** `scripts/import-luzzi.ts` con rimappaggio versificazione via TVTMS; genera `translations/luzzi.json` e aggiorna il manifest. Verificare fonte, edizione (1924/1927) e licenza; fissare l'anno nel `meta`.
  *Fatto quando:* controllo di completezza del validatore verde (nessun buco inatteso sui versetti TM).
  *Esito:* fonte eBible.org `ita1927` (USFM, pubblico dominio), anno 1927. 5852 versetti sorgente → 5852 testi su id TM, 245 rinumerati (Gen 32, Es 8, Es 22, Lv 5–6, Nm 17, Nm 30, Dt 13/23/29). Unico versetto TM scoperto: `num.25.19`, che la versificazione KJV ingloba in Num 26:1 — dichiarato in `meta.lacune` (campo nuovo, SCHEMI-DATI §2.7) invece di essere riempito arbitrariamente.
- [x] **F1.5 Import TSK.** `scripts/import-tsk.ts` → `crossrefs/` per libro, con flag `interno`.
  *Esito:* fonte TSK **Enhanced** v1.2 (`biblewebapp-master/input/com_tske/tske.txt`): non pubblico dominio ma copyright derivativo © 2010 Timothy S. Morton, ridistribuzione libera solo a titolo gratuito e in formato aperto — licenza reale nel `meta`, §2.8 aggiornato. 5633 versetti del Pentateuco, 87 507 tag `<u>` letti, range espansi versetto per versetto, 116 210 riferimenti scritti (54 525 interni). Rimappaggio TVTMS KJV→TM applicato a `da` (239) e ad `a` interni (1914). Scartati titoletti e glosse inglesi della sezione *Reciprocal* (nessun campo nello schema), tenuti i suoi riferimenti. 243 versetti TM restano senza riferimenti (soprattutto liste censuarie in Num).
- [x] **F1.6 Vista lettura.** In due passi, con le skill `frontend-design` e `ui-ux-pro-max` attive e i vincoli di `docs/DESIGN.md`:
  **(a) ⏸ APPROVAZIONE — Sistema di design.** Design token (colori/spaziature/tipografia, inclusa la semantica dei 5 status), scelta motivata della font latina entro i vincoli di DESIGN.md §3, self-hosting del font ebraico con verifica visiva su versetti reali importati, mock statico (HTML/CSS) della vista lettura. Solo dati importati o placeholder dichiaratamente finti nei mock. **Stop: si implementa solo dopo l'ok.**
  **(b) Implementazione.** Colonna centrale (ebraico word-level cliccabile + traduzione a fronte), pannello parola (parsing leggibile, glossa EN etichettata, occorrenze del lemma navigabili via indice), colonna sinistra (navigazione libro/capitolo, selettore traduzione), persistenza ultima posizione e traduzione in `localStorage`.
  *Fatto quando:* si legge Genesi in ebraico + Luzzi, ogni parola apre il pannello con occorrenze funzionanti, e tutti gli stili passano dai token (nessun valore esadecimale nei componenti).
  *Esito (a):* font self-hosted in `public/fonts/` — Ezra SIL 2.51 (OFL + MIT per la layout intelligence; solo TTF dal produttore, servito come TTF), Charis SIL 6.101 e IBM Plex Sans (OFL, woff2). Copertura verificata leggendo la `cmap`: Ezra copre niqqud e te'amim del campione, Charis copre tutti i diacritici di traslitterazione, **Plex non copre `ʾ ʿ ḏ ṯ ḥ ṭ ṣ`** → regola nei token: ogni traslitterazione usa `--font-translit` (Charis), il sans resta all'apparato UI. Token in `src/stili/tokens.css`; i 5 status hanno colore + stile di tratto + segno geometrico (■ ▣ □ ◊ △), `da_verificare` bordo tratteggiato + tratteggio diagonale. Mock statico in `public/mock/lettura.html` (generato dai dati importati). Approvato dall'utente.
  *Esito (b):* vista lettura in `src/viste/Lettura.tsx` + `src/componenti/`, stili in `src/stili/app.css` (0 esadecimali fuori dai token). Posizione e traduzione persistite in `localStorage` con ri-validazione in lettura. Le occorrenze del lemma vengono da `indices/lemmi.json`, caricato in modo pigro alla prima parola aperta (~2 MB) e limitato a 200 voci mostrate; la navigazione per occorrenza attraversa libro e capitolo e porta il versetto in vista. Il manifest delle traduzioni elenca solo gli id: nel selettore il nome esteso compare per la traduzione caricata. Verificato nel browser: Genesi 1 (31 versetti, 434 parole), pannello parola con parsing decodificato, salto a Genesi 31,11 da un'occorrenza, posizione ripresa dopo reload, nessun errore in console.
- [x] **F1.3-fix Correzioni a `import-tipnr.ts`.** Due difetti emersi controllando le bozze prima dello spostamento in `public/data/`.
  **(A) Candidati duplicati per coordinata.** L'unica fusione presente riguarda la sola coordinata TIPNR (`vicini()`); fra le risoluzioni OpenBible non c'è alcun confronto, e `idCandidato()` è un uniquificatore di slug, non un dedup: da lì i `X-2`/`X-3`. OpenBible ha più `identifications[]` per luogo, ciascuna col suo `score.time_total`, e appiattendole si ottiene lo stesso punto ripetuto con pesi diversi. Dedup per coordinata **esatta** (lat/lon a 4 decimali) dentro `costruisciLuogo`, primo vince — l'array è già ordinato per peso decrescente, quindi vince il peso massimo. I pesi non si sommano: sommare sarebbe un'inferenza, e sforerebbe l'1 dello schema. Non si usa `vicini()` (0,05° ≈ 5 km) perché fonderebbe proposte realmente distinte (i quattro *khirbet* di Ai stanno a 1–3 km l'uno dall'altro).
  **(B) Join con OpenBible.** *La diagnosi iniziale — l'ancora TIPNR porta il range dei riferimenti (`Gaza@Gen.10.19-Act`) mentre OpenBible linka il solo primo riferimento — era sbagliata: la chiave costruita dallo script è `nome@ancora`, il range sta in `nome.completo` e non entra nella chiave. Ritentare il match senza suffisso di range recupera 0 join.* La causa vera è che `linked_data` è **molti-a-molti**: lo stesso id TIPNR è rivendicato da più voci OpenBible (`Gaza@Gen.10.19` da `gaza` e da `ayyah`, `Babylon@Gen.10.10` da sette voci), su 52 dei nostri luoghi, e `perTipnr` risolveva la contesa col primo arrivato, cioè per ordine alfabetico del file. Da lì i candidati di Ai finiti su Gaza. Le voci si raccolgono tutte per id e la contesa si risolve sulla colonna «OpenBible name» di TIPNR — l'unico segnale che venga dalla sorgente e non dall'ordine; quando il nome non discrimina la scelta resta arbitraria. **Tutti** i 52 id contesi vanno loggati, non solo i 16 irrisolti: la disambiguazione è un'euristica su una sorgente ambigua, e c'è almeno un caso in cui la voce indicata da TIPNR è la più povera (vedi Ghicon nell'esito). Loggato per intero anche il ripiego per nome.
  *Prima di rigenerare:* estrarre le modifiche manuali già presenti in `bootstrap/` in `curation-override.json` indicizzato per `tipnr_id` (che è stabile), e riapplicarle dopo la rigenerazione — vedi l'avvertenza in coda a F1.3.
  *Fatto quando:* `npm run valida` verde, zero candidati con coordinata duplicata, e riepilogo con duplicati rimossi, join recuperati dal fix B, join ancora risolti per nome e luoghi di Gen 1–11 con candidati cambiati.
  *Esito:* nuovo `scripts/curation-override.ts` (`estrai` / `applica`) che salva e riporta la curation manuale per `tipnr_id`; round-trip verificato byte per byte prima di toccare l'import. Estratti 177 campi su 142 record (32 luoghi, 110 persone: `nomi.it`, `nomi.translit` corretti, 16 `status`, `pro`/`contro` di Eden/Eufrate/Tigri, la fonte Bible Odyssey di Eden), riapplicati tutti dopo la rigenerazione, 0 persi e 0 divergenti. **(A)** candidati 1018 → 920, duplicati per coordinata da 65 a **0**. **(B)** join per id invariato come totale (219) ma non come contenuto: 167 id con una sola voce, 36 contesi risolti sul nome, 16 contesi con scelta arbitraria, tutti e 52 elencati nel log; 35 luoghi su 244 hanno candidati diversi. Restano 20 luoghi agganciati col ripiego per nome e 5 senza corrispondenza OpenBible (verificato: nessuna normalizzazione della chiave li recupera, OpenBible non li linka proprio).
  *Da rivedere in curation (Gen 1–11):* `gaza` 18 → 1 candidato (Tell Harube, era la rosa di Ai) e `assyria` 2 → 1 (la regione Assiria, era la città Assur) sono correzioni; **`gihon` 8 → 1 è una regressione**: TIPNR tiene insieme in `Gihon@Gen.2.13-2Ch` il fiume dell'Eden e la sorgente di Gerusalemme, e la sua colonna «OpenBible name» punta a `gihon-2`, cioè alla sola sorgente — così le 8 proposte per il fiume (Nilo, Karkheh, Diyala, Karun…) sono sparite. Nessuna euristica può deciderlo: va scelto a mano quale voce OpenBible vale per Gen 2,13.

## Fase 2 — Contesto e curation Genesi 1–3

- [x] **F2.1 Bozze curation Gen 1–3.** ⏸ REVISIONE — `events` (segmentazione in pericopi completa e contigua su Gen 1–3, tre assi compilati), `notes`, integrazione/correzione di `places`/`people` per il range. Ogni claim con fonti reali o `da_verificare: true`. Mai inventare fonti o consenso.
  *Esito:* fonti concordate con l'utente: **Bible Odyssey** (SBL) per la parte storico-critica — Carr *The First Creation*, Halvorson-Taylor *The Second Creation*, Petersen *The Primeval History*, Callender *Eden* — e **Sefaria** per la tradizione ebraica. `bootstrap/events.json`: 8 pericopi contigue su Gen 1,1–3,24. `bootstrap/notes.json`: 22 note (17 storico-critiche, 5 `tradizione_ebraica`). `places`/`people`: compilati `nomi.it` e `nomi.translit` (convenzioni del progetto, non il formato TIPNR) per le 10 entità del range; `eden` portato da `disputed` a `symbolic` con `pro`/`contro` sui candidati mesopotamici — **decisione poi ribaltata in revisione (22/07/2026): `eden` è tornato `disputed`**, quindi i suoi 5 candidati OpenBible tornano marker in minimappa e, da F3.1, sulla mappa completa. I `pro`/`contro` sui candidati restano. Tutti i record restano `da_verificare: true` per la passata finale F5.3.
  *Termini Sefaria (verifica richiesta da SCHEMI-DATI §6):* la licenza è **per singola versione**, non per opera, ed è leggibile dall'API (`/api/v3/texts/...`, campo `license`) o dal pannello "About this Text". Verificato: Rashi nella versione Rosenbaum-Silbermann 1929-34 → *Public Domain*; Ibn Ezra ebraico "Piotrkow 1907-1911" → *Public Domain*, ma la traduzione inglese Strickman-Silver → *CC-BY-NC*; Ramban in traduzione Chavel → *CC-BY*. Nome e logo Sefaria sono marchi registrati. **Decisione:** le note memorizzano solo `sefaria_ref` più una sintesi originale in italiano, e linkano in uscita; nessun testo di commento viene copiato nel dataset, così la questione del caching non si pone. Se in futuro servisse il caching, va verificata la licenza della singola versione citata.
  *Cesura Gen 2,4:* il versetto-cerniera (1,1–2,4a P / 2,4b– secondo racconto) non è divisibile con id al versetto; il v. 4 sta per intero nella pericope del secondo racconto e la doppia attribuzione è spiegata nella nota `gen24-cesura-versetto-cerniera`.
  *Aperto:* `composizione.range` del materiale non sacerdotale è un intervallo largo e provvisorio (-900/-400) perché le fonti usate non danno datazione assoluta — l'avvertenza è dentro le `sintesi`, la datazione va fissata in F5.3. `dati_narrativi` di `adam` resta `null`: le età di Gen 5 appartengono a F3.3/F3.4.
- [x] **F2.2 Colonna contesto.** Tab Dove/Quando/Chi sincronizzati con la pericope visibile durante lo scroll, con miniature (minimappa con stile marker per status; tre assi in miniatura; mini-schede persone). Installazione Leaflet (e D3 se serve già qui).
  *Esito:* installato **Leaflet 1.9** (`leaflet` + `@types/leaflet`), **D3 no**: gli assi in miniatura sono tre barre CSS su scala lineare, D3 entra con la timeline completa di Fase 3. Nuovi `src/componenti/ColonnaContesto.tsx` e `Minimappa.tsx`, `src/lib/pericopi.ts` (pericope che contiene un versetto, etichette di range e di anni) e `src/lib/confidenza.ts` (resa italiana dei 5 valori, riusabile in F2.4); loader e hook per `events`/`places`/`people`.
  *Sincronizzazione:* l'unità non è il versetto ma la **pericope** che lo contiene — è a quella che la curation attacca luoghi, tempi e persone. Il versetto "in lettura" è il primo che intercetta la fascia alta della finestra (`IntersectionObserver` con `rootMargin: 0 0 -55% 0`), senza listener di scroll. La colonna destra è diventata `sticky` sopra i 1100px: prima usciva dallo schermo appena si scorreva, il che rendeva inutile qualunque sincronizzazione.
  *Tre assi:* tre sezioni separate da filetto, ciascuna con la sua scala e la sua etichetta, mai una linea sola. La miniatura di un asse si disegna solo se il dominio esiste **nei dati** (min/max su tutta la curation): su Gen 1–3 `tempo_narrato` e `tempo_storico` non hanno ancoraggi, quindi resta la sola barra della composizione, e le altre due dicono a parole perché non c'è niente da collocare. Nessuna scala scelta a tavolino.
  *Luoghi `symbolic` fuori dalla carta* (la regola vale ancora; l'esempio non più: `eden` è tornato `disputed` in revisione, vedi F2.1)*:* Eden era `symbolic` pur avendo 5 candidati OpenBible; metterli come marker rimetterebbe in gioco una localizzazione che la curation ha escluso. I marker sono solo per gli status non simbolici; le ipotesi restano nell'elenco testuale col loro `peso_openbible`, e la scheda dichiara perché il luogo non è sulla carta. Il marker porta lo status del luogo con colore **e** stile di tratto (token `--status-*`), la scheda ripete il segno geometrico.
  *Assenza come dato:* passo fuori dai capitoli curati, pericope senza luoghi o senza persone, id citati dalla pericope ma assenti dai file — ognuno ha la sua frase esplicita invece di un pannello vuoto.
  *Correzione (durante F3.1): la sincronizzazione allo scroll non partiva al primo caricamento.* L'`IntersectionObserver` si costruiva appena arrivavano i versetti da `verses/`, ma la colonna di lettura si disegna solo quando è pronto anche `words/`: al momento dell'osservazione nella pagina non c'era ancora nessun versetto, si osservavano zero elementi e l'effetto non veniva più rifatto, perché l'elenco dei versetti non cambiava più. Il contesto restava così fermo sulla prima pericope del capitolo, e tornava a funzionare solo cambiando capitolo — allora l'elenco cambiava con i file già in cache, e la colonna era già disegnata. `usaVersettoInLettura` riceve ora anche `testoMontato` (le stesse due condizioni con cui la vista sceglie di disegnare la colonna) e lo tiene fra le dipendenze.
  *Correzione a un difetto di F2.5:* il riconoscimento del file `[C]` non installato si basava sul 404, ma il dev server di Vite (e ogni host in modalità SPA) risponde **200 con l'index.html**. L'assenza arrivava all'utente come "JSON malformato". Ora `scarica()` tratta come `RisorsaAssente` sia il 404 sia una risposta `text/html`, e la degradazione morbida vale per lexicon ed events/places/people.
  *Verifica:* colonna sincronizzata sulle 8 pericopi di Gen 1–3, minimappa con 31 marker su Gen 2,4–17, tab con navigazione a frecce e `role="tab*"`, nessun errore in console, `npm run valida` verde e build ok. La verifica è stata fatta caricando la curation reale da `bootstrap/` tramite un'istanza dell'app in iframe con `fetch` reindirizzato: `public/data/` non è stata toccata (regola 3).
  *Aperto:* i file `[C]` non sono ancora in `public/data/`, quindi nell'app la colonna dice "nessuna curation installata" finché non li sposti. Nelle mini-schede, le persone fuori dal range curato (es. i figli di Adamo) compaiono con la traslitterazione TIPNR (`He.vel`, `Ka.yin`) perché `nomi.it` è vuoto: si riempie con la curation dei rispettivi capitoli.
- [x] **F2.3 Traduzione letterale Gen 1–3.** ⏸ REVISIONE — Proposta pericope per pericope in sessione, approvazione esplicita dell'utente, solo poi scrittura in `translations/letterale.json`. Ambiguità reali → note, non risolte in silenzio.
  *Esito:* 80 versetti (Gen 1,1–3,24), approvati pericope per pericope sulle 8 di `events.json`. Bozze in `bootstrap/translations/letterale.json` + `bootstrap/translations/index.json` e `bootstrap/notes-f23.json` (26 note: 19 filologiche, 7 divergenze traduttive) — regola 3: **non** scritte in `public/data/`. Criterio: letterale con italiano leggibile; ogni resa ricostruibile da TAHOT (morfologia) e TBESH via `lexicon_it.json`, nessuna da altre traduzioni.
  *Convenzioni fissate dall'utente, tutte dichiarate in `meta.note` della traduzione:* tetragramma → «il Signore»; `אֵת` non tradotto; participi predicativi senza copula, normalizzati solo dove incomprensibili; nifal al medio, passivo dove il medio non esiste; `בֵּין…וּבֵין` con doppio «tra»; `שָׁמַיִם` sempre plurale; infinito assoluto reso col calco («mangiare mangerai»), perché Gen 3,4 lo nega parola per parola; `טוֹב`/`רַע` aggettivi («buono e cattivo», non «bene e male»); `נֶפֶשׁ חַיָּה` sempre «anima vivente» per non spezzare il legame Gen 1 → Gen 2,7; coppie `אֶרֶץ`/`אֲדָמָה` e `בָּרָא`/`יָצַר`/`עָשָׂה`/`בָּנָה` sempre distinte; toponimi e antroponimi da `places.json`/`people.json`, non dalle glosse.
  *Traslitterazione in linea:* solo dove il testo motiva un nome con un'altra parola (2,23 `ʾiššâ`/`ʾîš`; 3,20 `ḥawwâ`/`ḥay`), in forma accademica come nel resto del dataset. Fuori da quei casi mai: una traduzione sceglie, e la trasparenza sta nel rendere la scelta visibile e attribuita, non nel rifiutarla.
  *Note ancorate alla parola:* 19 delle 26 usano il target `parola` di `TargetNota` (`ParolaId`), che era nello schema da F0.2 e non era mai stato usato — le 26 note curate puntavano tutte a versetti, pericopi, luoghi o persone. È il meccanismo che rende la divergenza traduttiva visibile sul punto esatto in cui nasce; la resa in UI è F2.4.
  *Fonti:* ogni nota cita il dato verificabile nel dataset (codice morfologico TAHOT o glossa TBESH, con la sigla esatta) e, dove afferma qualcosa sulla tradizione traduttiva italiana, cita la **Luzzi già importata** invece di generalizzare a memoria. Tutte `da_verificare: true` per F5.3.
  *Aperto:* (a) **contraddizione dichiarata** fra la letterale e la nota curata `gen320-nomi-propri-tardivi` — la traduzione scrive «Adamo» in Gen 2,20; 3,17; 3,21 seguendo il tagging `Npm` di TAHOT, la nota dice che il nome proprio compare solo da Gen 4,1: `gen220-adam-nome-proprio` registra il conflitto, va sciolto in F5.3 in un senso o nell'altro. (b) `gen116-luminari-senza-nome` e `gen22-terminare-nel-settimo-giorno` affermano **solo il dato testuale**: l'interpretazione antiastrale e le letture di altre tradizioni testuali restano fuori perché bibleodyssey.org rispondeva 403 e nessuna fonte è stata verificata. (c) `gen26-ed-vapore` idem sulle rese alternative di `אֵד`. (d) `translations/index.json` di `public/data/` va esteso con `"letterale"` al momento dello spostamento, altrimenti il validatore segnala la traduzione non elencata nel manifest.
  *Modifica a `valida.ts`:* il classificatore riconosceva le collezioni curate solo per nome esatto, quindi `notes-f23.json` non aveva schema. Ora accetta anche `<collezione>-<suffisso>.json` per notes/places/people/events: la curation procede per range e una bozza parziale va tenuta distinta dal file revisionato senza uscire dalla validazione. Il rischio di sovrapposizione è già coperto dall'avviso sugli id duplicati fra file.
- [x] **F2.4 Note in UI.** Indicatori a margine per tipo/confidenza, pannello nota con badge, fonti, flag `da_verificare`; note `tradizione_ebraica` con link Sefaria (verifica dei termini d'uso già fatta in F2.1: solo `sefaria_ref` e link in uscita, nessun testo di commento nel dataset).
  *Esito:* nuovi `src/lib/note.ts` (riduzione dei cinque target a "che cosa compare a margine di quale versetto", etichette di tipo e prospettiva, URL Sefaria), `src/componenti/PannelloNote.tsx`, `src/componenti/Elementi.tsx` (badge, segno di status, "da verificare", elenco fonti: erano funzioni private di `ColonnaContesto`, ora condivise perché la grammatica della confidenza deve essere scritta in un posto solo), più loader e hook `notes.json` con la stessa degradazione morbida degli altri file `[C]`.
  *Due canali per due dati:* l'indicatore a margine porta una **lettera** per il tipo (F filologica, S storica, G geografica, D divergenza traduttiva, T tradizione ebraica) e accanto il **segno geometrico** della confidenza già usato da mappa e badge (■ ▣ □ ◊ △, ○ per `attribuito`). Il colore ripete la confidenza ma non la porta mai da solo. La stessa sigla ricompare nel pannello accanto al tipo per esteso, così il margine si impara leggendo, senza legenda a parte. `--fascia-margine` passa da 1.75rem a 2.5rem per ospitarli: la misura di lettura non cambia, si sposta la colonna.
  *Riduzione dei target:* `parola` → il versetto che la contiene (e la parola resta marcata nel testo); `pericope` → **solo il primo versetto** del range, perché un segno ripetuto per dieci versetti direbbe "dieci note"; `luogo` e `persona` non hanno un versetto a cui stare e si raggiungono dal rimando dentro la loro scheda nella colonna contesto — senza quel rimando sarebbero note irraggiungibili.
  *Note ancorate alla parola (le 19 di F2.3):* la parola annotata porta un tratteggio sotto la riga, spinto in basso per non toccare i te'amim; il click apre insieme pannello parola e pannello note. Nessun bersaglio cliccabile aggiunto dentro il testo ebraico: un segno in linea romperebbe il flusso rtl.
  *Sefaria:* forma dell'URL verificata sul sito (200 diretto, senza redirect) — spazi in underscore, `:` in punto, punto anche davanti alla prima cifra: `Rashi on Genesis 1:1:1` → `Rashi_on_Genesis.1.1.1`; verificato anche su `Ibn Ezra on Genesis 1:1:1` e `Bereshit Rabbah 23:3`. Si linka e basta: del commento non entra nel dataset nemmeno una riga.
  *Scelte minori:* il pannello si apre solo su richiesta esplicita (le note sono apparato, non commento continuo) e si riporta in vista la nota scelta, non la testa del pannello, perché la colonna dell'apparato scorre per conto suo; nelle note `tradizione_ebraica` il tipo non si ripete sotto l'etichetta di prospettiva; il `dettaglio` delle fonti (pagina, sezione, codice morfologico) compare nel pannello note e resta fuori dalle miniature del contesto.
  *Verifica:* Gen 1 con le 26 note di `public/data/notes.json` (11 indicatori, pannello con prospettiva, badge, ancoraggio, fonti, `da verificare`, link Sefaria corretti), e le 19 note di parola provate caricando anche `bootstrap/notes-f23.json` con un'istanza in iframe e il `fetch` reindirizzato — `public/data/` non è stata toccata. Nessun errore in console, `npm run build` e `npm run valida` verdi.
  *Aperto:* su un versetto molto breve con molte note la pila di indicatori può sconfinare nella fascia del versetto successivo (oggi non accade: il massimo è 5 su Gen 1,1).
  *Dataset allineato dopo il task (22/07/2026):* le 26 note di F2.3 sono state unite in `public/data/notes.json` (52 note, id tutti unici) e `bootstrap/notes-f23.json` cancellato; `translations/letterale.json` spostata in `public/data/translations/` con `"letterale"` aggiunto al manifest (era il punto (d) rimasto aperto in F2.3); `eden` riportato a `disputed`. `bootstrap/` è ora vuota.
- [x] **F2.5 Glosse italiane per lemma.** ⏸ REVISIONE Creazione di `public/data/lexicon_it.json` `[C]` (SCHEMI-DATI §2.9, schema già in `src/tipi/lemma.ts`) popolato con i lemmi di Gen 1–3, più il caricamento nel pannello parola: dove esiste una glossa italiana si mostra quella, con la glossa EN di TAHOT che resta visibile ed etichettata come tale (mai sostituita in silenzio). Ogni voce porta `fonti`: la glossa TAHOT da cui deriva e/o un lessico con licenza compatibile — nessuna glossa scritta a memoria. Da F2.5 in poi ogni task di curation estende il file sul proprio range (vedi regole di esecuzione).
  *Nota:* tocca la UI, quindi vale la regola delle skill `frontend-design` e `ui-ux-pro-max` e i vincoli di `docs/DESIGN.md`.
  *Esito:* **fonte cambiata rispetto al testo del task, su ok esplicito dell'utente.** La base non è la `glossa_en` di `indices/lemmi.json` ma **TBESH** (*Translators Brief lexicon of Extended Strongs for Hebrew*, STEPBible / Tyndale House, CC BY 4.0), già in `scripts/sources/`: dà una glossa **per dStrong** anziché per occorrenza di parola, copre 272/272 i lemmi lessicali di Gen 1–3, e mantiene la struttura `generale: senso specifico` che è ciò che tiene distinti i dStrong disambiguati (`H0776G` "terra: paese/pianeta" vs `H0776H` "terra: suolo"). 61 glosse su 272 divergono da quelle a livello di parola, con casi in cui TAHOT è troncato o errato (`H7704G` שָׂדֶה glossato `country; planet`, cioè la glossa di אֶרֶץ; `H3372G` `frightening(DANGER)`; `H8248G` `watering(ANIMALS`). Si usa **solo la colonna `Gloss`** ("created by Tyndale scholars"): la colonna successiva, derivata dall'*Abridged BDB* di Online Bible, porta una restrizione d'uso esplicita nel preambolo del file e non entra nel dataset in nessuna forma.
  Bozza in `bootstrap/lexicon_it.json` (regola 3: **non** scritta in `public/data/`), 272 voci, tutte `da_verificare: true` per la passata F5.3. Assemblata da `scripts/bozza-lexicon-it.ts`: le glosse italiane sono curate a mano e stanno in chiaro nello script, che non traduce nulla e si limita a leggere da TBESH il testo inglese esatto da cui ogni resa deriva, incollandolo nel `dettaglio` della fonte — così la catena italiano → inglese → dStrong è verificabile senza fidarsi di una trascrizione. Esclusi i ~28 morfemi grammaticali `H9xxx` (prefissi, suffissi, pronomi legati): sono già resi in italiano da `src/lib/morfologia.ts` e duplicarli creerebbe due fonti di verità.
  UI: `caricaLexiconIt()` tratta il **404 come "nessuna glossa ancora disponibile"** (lexicon vuoto), non come errore — è la condizione normale finché la bozza non è stata revisionata e spostata. Nel pannello parola le due glosse convivono sempre, ciascuna con la sua etichetta di provenienza: l'italiano sopra in inchiostro pieno, la EN di TAHOT sotto in inchiostro medio (gerarchia per peso di colore, stessa forma per entrambe). Il `da_verificare` usa il linguaggio "non finito" di DESIGN.md §4 riportato alla scala del testo in linea — sottolineatura tratteggiata in `--verificare-colore` più testo per lettori di schermo — invece del bordo tratteggiato pieno, che su ogni glossa di una fase in cui tutto è ancora da verificare sommergerebbe il pannello.
  *Difetto noto in `indices/lemmi.json`, da correggere in un task dedicato di fix dell'import (fuori dallo scope di F2.5):* per due lemmi la forma presa a livello di parola da TAHOT è sbagliata o incompleta rispetto a TBESH — `H1419A` risulta `גַּל` invece di `גָּדוֹל`, `H3068G` risulta `יהוה` senza vocalizzazione invece di `יְהֹוָה`. Il pannello parola mostra quindi il lemma sbagliato per queste due voci.

## Fase 3 — Viste complete e curation Genesi 4–11

- [x] **F3.1 Mappa completa.** Tutti i luoghi del range curato, filtri per status e capitolo, popup con candidati alternativi e fonti.
  *Esito:* nuovi `src/viste/Mappa.tsx`, `src/componenti/MappaCompleta.tsx` e `src/lib/luoghi.ts`; `App.tsx` scambia le due viste con uno stato (niente router: non ci sono URL da condividere e una dipendenza in più va chiesta prima). Si entra dalla scheda di un luogo nel tab Dove («Vedi sulla mappa», che apre già quel luogo) o dal rimando sotto la minimappa («Apri la mappa completa»); si esce con «Torna alla lettura» o da un riferimento della scheda, che riporta il testo su quel versetto riusando il meccanismo della navigazione per occorrenza.
  *Che cosa entra nella carta:* i luoghi **nominati dalle pericopi curate** (8 su Gen 1–3, 28 ipotesi), non i 244 record di `places.json` — quelli vengono da TIPNR e non hanno ancora un giudizio della curation. Il pannello lo dichiara invece di lasciarlo dedurre. Resta la regola di F2.2: `symbolic` e luoghi senza candidati non vanno sulla carta, e per loro l'apparato completo sta nella scheda dell'elenco, che è l'unico posto dove può stare (oggi nessun luogo curato ricade in questo caso: `eden` è tornato `disputed`, quindi il ramo non è verificato sui dati veri).
  *Un marker è un'ipotesi, non un punto:* più marker possono appartenere allo stesso luogo. Quando un luogo è aperto, **tutte** le sue ipotesi prendono l'anello nel colore del suo status e vengono legate da raggi tratteggiati dello stesso colore al loro baricentro, mentre gli altri marker arretrano di opacità; dentro l'insieme, l'ipotesi di cui il popup sta parlando ha l'anello più spesso e in inchiostro, così la differenza non sta nel solo colore. Senza quel segno, gli 11 candidati di `pishon` sembrerebbero undici luoghi. Aprendo un luogo **dall'elenco** (o arrivando dalla colonna contesto) la carta inquadra tutte le sue ipotesi: di lì non si sa dove cadano. Da un click sul marker no — quel punto lo si sta già guardando, e una carta che salta a ogni click disorienta. Il popup mostra tutte le ipotesi del luogo con il loro peso OpenBible, espande solo quella aperta (pro, contro, fonti con `dettaglio`) e permette di passare da una all'altra.
  *Filtri per sottrazione:* si parte da tutto visibile e si escludono status e capitoli, così lo stato iniziale non dipende dal caricamento dei dati e «nessun filtro» non si confonde con «nessun risultato». Ogni voce porta il suo conteggio; il luogo aperto che esce dai filtri si chiude da sé.
  *Popup in React:* il contenuto del popup è un portale su un div stabile passato a Leaflet, non una stringa HTML — badge, segni di confidenza ed elenco fonti restano quelli di `Elementi.tsx`. Il fuoco entra nel popup solo dopo che Leaflet lo ha attaccato al documento (`data-fuoco`), ed Esc lo chiude riportando il fuoco alla scheda di partenza: la scorciatoia di Leaflet non basta, vale solo col fuoco sulla carta.
  *Note curate dei luoghi:* stanno nel popup (e nella scheda dell'elenco per i luoghi non collocabili, che popup non ne hanno), con lo stesso apparato del pannello della lettura — `VoceNota` è stata esportata da `PannelloNote.tsx` invece di riscrivere prospettiva, sigla, badge e fonti una seconda volta. L'elenco porta il conteggio («· 1 nota») così si vede quali luoghi hanno curation senza aprirli uno per uno, e dove la nota manca lo si scrive: è un dato sullo stato della curation, non un vuoto. Il popup si allunga fino a 60vh perché venti righe non bastavano più.
  *Il click sulla carta non chiude il popup* (`closeOnClick: false`). Emerso da un difetto vero, trovato solo con click reali: scegliendo un'altra ipotesi, React rifà la riga cliccata, il bersaglio del click sparisce dal documento e Leaflet — che risale la catena dei genitori per capire se il click veniva dal popup — non la trova più, quindi lo trattava come un click sulla carta e chiudeva tutto. Oltre a togliere il difetto, la scelta è giusta di suo: il popup è apparato che si sta leggendo, e chiuderlo perché il puntatore ha toccato il mare è una perdita. Restano la X e Esc.
  *Verifica:* filtri (4 su 8 escludendo `consenso`; 1 escludendo anche Genesi 2), popup di `pishon` con 11 ipotesi, cambio di ipotesi e passaggio a un altro luogo **con click reali**, raggi e opacità, X ed Esc che chiudono e deselezionano (Esc riporta il fuoco alla scheda), nota di `eden` nel popup e conteggio nell'elenco, «Torna alla lettura» e rimando a Genesi 2,10 che riapre il testo sul versetto. Nessun errore in console, `npm run build` e `npm run valida` verdi.
  *Aperto:* nessuno.
- [x] **F3.2 Timeline a tre binari.** D3; incertezza resa come range/sfumature, mai punti secchi; binari etichettati.
  *Esito:* nuovi `src/viste/Timeline.tsx`, `src/componenti/BinarioTempo.tsx` e `src/lib/tempo.ts`; `App.tsx` guadagna la terza vista con lo stesso meccanismo della mappa (nessun router). Si entra dal tab «Quando» della colonna contesto («Apri la timeline completa», che apre già la pericope in lettura); si esce con «Torna alla lettura», con i riferimenti interni del tempo narrato o con «Leggi … nel testo», che riportano il testo su quel versetto.
  *Dipendenza:* installato **`d3-scale`** (+ `@types/d3-scale`), non il pacchetto `d3` intero: serve la matematica della scala e la scelta delle tacche, il DOM lo scrive React come per il popup della mappa. Un `d3.select` su nodi che React possiede sarebbe due padroni sullo stesso albero. `d3-hierarchy` si valuterà in F3.3.
  *Tre binari, tre scale:* ogni binario calcola il **suo** dominio sui suoi dati e porta la sua unità di conto accanto al titolo (Anno Mundi / anni a.C.-d.C.). Nessun asse condiviso e nessun allineamento verticale significante: l'avvertenza in testa alla vista lo dice per esteso, perché tre grafici impilati invitano a leggere una colonna che non esiste. L'unica cosa che i binari condividono è l'ordine delle corsie, che è quello del testo — ed è ciò che rende leggibile il confronto: la stessa pericope, tre collocazioni diverse.
  *Due segni per due affermazioni:* `blocco` pieno per il tempo narrato (durata dentro il racconto, estremi netti perché il testo li dà) e `forcella` — due limiti e ciò che sta in mezzo, senza riempimento — per ancoraggio storico e composizione, che sono intervalli di incertezza con distribuzione ignota. Un blocco pieno lì direbbe che il valore occupa tutto l'intervallo. Sul solo asse che porta una confidenza (`tempo_storico`) la forcella prende colore **e** tratteggio dello status: in SVG il `border-style` dei token non esiste, il `stroke-dasharray` ne è la trasposizione, la regola «colore e forma, mai il solo colore» resta. Se i due estremi coincidono la figura si stringe quanto il dato: allargarla per non farla sembrare un punto disegnerebbe un'incertezza che i dati non dichiarano.
  *Nessun dominio, nessuna scala:* su Gen 1–3 `tempo_narrato.am` e `tempo_storico.ancoraggio` sono `null` su tutte e 8 le pericopi, quindi i due binari **non disegnano un righello**: dichiarano «0 di 8 collocate» e rimandano alla scheda, dove la curation dice il perché con la sua confidenza (l'assenza di ancoraggio è `consensus`, cioè un'affermazione). È la stessa regola dei mini-assi di F2.2. Il ramo con dominio è verificato sui dati veri dalla composizione (8 su 8, forbice -900/-400).
  *L'elenco è la fonte:* come nella mappa, tutto ciò che sta sui binari sta anche a sinistra in parole — le tre collocazioni sono righe di testo sulla scheda chiusa, e la scheda aperta porta sintesi, badge di confidenza, ventaglio delle posizioni, fonti con `dettaglio`, note curate della pericope (`VoceNota` riusata) e `da_verificare`. La figura è una sintesi, non l'unico accesso: l'SVG è `role="img"` con la sua descrizione, e la navigazione da tastiera passa dall'elenco.
  *Note della pericope:* solo quelle con target `pericope` contenuto nel range (6 su Gen 1–3), non tutte quelle dei versetti compresi — quelle sono apparato della lettura e qui sommergerebbero la scheda.
  *Rifattorizzazione minima condivisa:* testata e apparato delle viste a schermo pieno passano da `.mappa-testa`/`.mappa-apparato` a `.vista-testa`/`.vista-apparato` (e `--col-mappa` → `--col-apparato`), perché mappa e timeline hanno la stessa impalcatura e DESIGN.md §6.4 chiede che le viste riusino, non reinventino.
  *Verifica:* ingresso dal tab «Quando» con la pericope già aperta, click reali su due corsie diverse del binario composizione (selezione che si sposta, corsia marcata su tutti i binari, scheda che si espande e si porta in vista), rimando a Genesi 1,8 che riapre il testo sul versetto, «Torna alla lettura», mappa ancora integra dopo la rinomina delle classi. Nessun errore in console, `npm run build` e `npm run valida` verdi.
  *Verifica dei rami che la curation reale non tocca:* su Gen 1–3 narrato e storico sono vuoti, quindi blocco pieno, forcella colorata per status e corsia «senza collocazione» dentro un binario **con** scala non sarebbero mai stati esercitati. Provati con un fixture **dichiaratamente finto** (5 pericopi «PROVA n — dato finto», nessuna fonte, nessun claim) iniettato a runtime in un'istanza dell'app in iframe con `fetch` reindirizzato: `public/data/` e `bootstrap/` non sono state toccate e il banco di prova è stato cancellato a verifica finita (regole 2 e 3). Esito: blocchi con larghezza proporzionale e minimo di 3 px sugli estremi coincidenti; asse in «AM» distinto da quello in a.C.; le cinque confidenze con colore **e** tratto distinti (`consensus` pieno, `majority` pieno sottile, `disputed` 5-3, `speculative` 1-3 a punti tondi, `symbolic` 7-2-1-2); corsia senza collocazione scritta a parole e ancora selezionabile, con la marcatura che resta visibile su tutti e tre i binari. **Nota sul metodo:** l'iframe va montato con `srcdoc`, non con `document.write` — `document.open()` azzera i listener della finestra e il documento scritto viene poi sostituito dal caricamento dell'about:blank iniziale (l'app si monta e sparisce senza lasciare errori); dentro `srcdoc` serve anche il preambolo di `@vitejs/plugin-react`, che in dev Vite inietta solo nelle pagine HTML che serve lui.
  *Aperto:* (a) con un solo asse popolato **dai dati veri** la vista è per ora un grafico e due dichiarazioni di assenza: il codice dei tre binari è verificato (vedi sopra), ma come si legga un binario narrato *pieno* — quante corsie regga, se le durate di Gen 5 schiaccino tutto il resto — si giudicherà solo con la curation di Gen 5 e 11 (F3.4). (b) **Ambito della vista, deciso con l'utente (22/07/2026):** la timeline mostra *tutto* ciò che sta in `events.json` — oggi Gen 1–3, con F3.4 Gen 1–11 — e questo resta il comportamento di default. Quando la curation si estenderà, servirà un **controllo di ambito** (libro, intervallo di capitoli) come i filtri già presenti nella mappa: una corsia per pericope regge otto righe, non le centinaia di un Pentateuco curato per intero. Il componente del binario è già indifferente al libro (ordina per posizione nel testo e non conosce Genesi); il lavoro sta nel selettore e nel modo in cui l'apparato dichiara che cosa si sta guardando. Un'estensione oltre il Pentateuco — ipotesi dell'utente, fuori dallo scope della specifica — non toccherebbe la timeline ma le convenzioni di id e `LIBRI` in `src/lib/riferimenti.ts`, dove tutto ciò che sta fuori dai cinque libri restituisce `null` per scelta (F1.x).
- [ ] **F3.3 Genealogie.** Alberi Gen 5, 10, 11; età letterali come dato narrativo; note critiche agganciate; navigazione persona→versetti.
- [ ] **F3.4 Curation Gen 4–11.** ⏸ REVISIONE — stesso processo di F2.1, in blocchi piccoli (es. 4–6, 7–9, 10–11).
  - [ ] **Blocco Gen 4–5 — bozze pronte, in attesa di revisione (22/07/2026).** In `bootstrap/`: `events-gen45.json` (4 pericopi contigue su Gen 4,1–5,32), `notes-gen45.json` (14 note: 12 storico-critiche o filologiche, 2 `tradizione_ebraica`), `places-gen45.json` (il solo `nod`), `lexicon_it.json` (108 glosse nuove). **Non** scritte in `public/data/`: lo spostamento è dell'utente dopo la revisione (regola 3).
    *Perché 4–5 e non 4–6:* il validatore pretende copertura contigua sul range curato, quindi non si può curare Gen 5 da sola per far comparire le cifre; 4–5 è il blocco più corto che porta l'Anno Mundi nella timeline.
    *Fonti verificate in sessione (tutte lette, non citate a memoria):* Pamela Barmash, *Cain*; David L. Petersen, *The Primeval History*; Martien A. Halvorson-Taylor, *The Second Creation*; Kelley Coblentz Bautch, *Enoch* — tutte Bible Odyssey (SBL); la voce *genealogy* dell'HarperCollins Bible Dictionary su Bible Odyssey; per la tradizione ebraica Rashi su Gen 4,7 e Gen 5,24 letti dall'API Sefaria nella versione Rosenbaum-Silbermann 1929-34 (licenza dichiarata dall'API: Public Domain), sintetizzati in italiano e linkati, senza copiare testo nel dataset. **`bibleodyssey.org` risponde 403 a WebFetch:** gli articoli si leggono dal browser vero, non dallo strumento di fetch.
    *Cronologia in Anno Mundi:* le cifre non sono state scritte a mano né ricordate — sono le `dati_narrativi.eta_al_primo_figlio` già estratte da TIPNR in `people.json`, sommate. Con la creazione di Adamo posta ad anno 0: Set nel 130, Noè nel 1056, il suo cinquecentesimo anno nel 1556 — convenzione coerente con l'esempio di SCHEMI-DATI §2.5, dove il diluvio (Noè a 600 anni) cade nel 1656. La convenzione è dichiarata nella `nota` della pericope e in una nota curata, perché spostare l'anno di partenza sposta tutte le cifre.
    *Che cosa non entra nelle bozze:* l'attribuzione formale di Gen 5 alla cornice sacerdotale, l'argomento delle fonti sul nome divino in Gen 4,26 e il confronto TM/LXX/Samaritano sulle età sono **dichiarati come non asseriti** dentro le `sintesi`, perché sono posizioni di manuale per cui non è stata verificata una fonte diretta. La nota `gen41-nomi-propri-da-qui` porta invece un elemento nuovo alla contraddizione aperta in F2.3 (`gen220-adam-nome-proprio`): Halvorson-Taylor sostiene la lettura «nomi propri solo da Gen 3,20 e 4,1».
    *Persone:* nessuna bozza necessaria. `people.json` da TIPNR ha già nomi italiani, parentele reciproche ed età per tutte e venti le figure di Gen 4–5.
    *Verifica in vista (senza installare le bozze):* app in iframe con `fetch` reindirizzato che unisce `public/data/` e `bootstrap/`. Esito: 12 pericopi su Genesi 1,1–5,32, binario narrato con la sua prima scala reale (blocco 130–1556 AM, «1 di 12 collocate»), binario storico ancora a «0 di 12» come previsto, colonna contesto sincronizzata su Gen 4 e 5, Nod nel tab Dove con la sua nota, le quattro note di Gen 5 a margine con sigla e confidenza giuste.
    *Ritocco a F3.2 emerso solo qui:* con dodici corsie, la frase «senza collocazione su questo asse» ripetuta su ogni riga vuota era un muro di testo che copriva l'unica collocata. Ora le corsie vuote portano un trattino e la frase per esteso compare sulla sola corsia scelta; l'assenza resta dichiarata dal conteggio sotto il grafico e dalla scheda della pericope.
  - [ ] **Blocco Gen 6–9 — bozze pronte, in attesa di revisione (22/07/2026).** In `bootstrap/`: `events-gen69.json` (6 pericopi contigue su Gen 6,1–9,29), `notes-gen69.json` (14 note: 12 storico-critiche o filologiche, 2 `tradizione_ebraica`), `places-gen69.json` (il solo `ararat`), più 151 glosse aggiunte a `bootstrap/lexicon_it.json` (259 in tutto con quelle di Gen 4–5).
    *Perché 6–9 in un blocco solo:* il ciclo del diluvio è un'unità narrativa e spezzarlo a metà avrebbe prodotto pericopi arbitrarie.
    *Fonti verificate in sessione:* Jenna Kemp, *Noah*; Joseph Angel, *Nephilim*; Mark E. Biddle, *The Curse of Ham*; più Petersen, *The Primeval History*, già usato nel blocco precedente — tutte Bible Odyssey (SBL), lette dal browser perché il dominio risponde 403 a WebFetch. Per la tradizione ebraica, Rashi su Gen 6,2 (i «figli di Elohim» come figli di potenti, da Bereshit Rabbah 26,15) e su Gen 6,9 (la doppia lettura di «nelle sue generazioni», con rimando a Sanhedrin 108a), letti dall'API Sefaria nella versione Rosenbaum-Silbermann, Public Domain.
    *Cronologia:* il diluvio cade nel **1656** (Noè a 600 anni, gen.7.6.11, sulla nascita nel 1056 calcolata dal blocco precedente) e la morte di Noè nel **2006** (950 anni, gen.9.29). La nota `gen7-come-si-datano-diluvio-e-morte-di-noe` registra uno **scarto interno al testo**: i 350 anni «dopo il diluvio» di gen.9.28 sommati ai 600 danno i 950, quindi si contano dall'anno d'inizio del diluvio, mentre la terra si asciuga nell'anno successivo (gen.8.13). Un anno di differenza fra due modi di contare che il testo non ricompone, e che la curation dichiara invece di appianare.
    *Doppiezze del racconto:* due animali per specie (gen.6.19) contro sette coppie di puri (gen.7.2), quaranta giorni di pioggia contro centocinquanta di acque alte — registrate come dato testuale, con l'osservazione di Petersen sui due racconti intrecciati. **L'assegnazione di ciascuna cifra a un materiale o all'altro non è asserita:** le pericopi del diluvio portano una sola forbice di datazione su un testo intrecciato, semplificazione dichiarata dentro le `posizioni` e da rivedere.
    *Ricezione tenuta distinta dal testo:* per Gen 6,1-4 la nota `gen64-vigilanti-lettura-posteriore` dice che gli angeli caduti vengono dal Libro dei Vigilanti e stanno «in opposizione» al racconto biblico (Angel); per Gen 9,25 la nota `gen925-maledizione-di-canaan-e-suoi-usi` riporta i due dati che smontano la lettura schiavista — è Canaan a essere maledetto, non Cam, ed è Noè a parlare, non Dio (Biddle).
    *Verifica in vista:* stessa anteprima in iframe del blocco precedente, con le due bozze unite ai file curati. Esito: **18 pericopi su Genesi 1,1–9,29**, binario narrato con 4 collocate su 18 — la lunga fascia della genealogia (130–1556), il punto del diluvio (1656), l'anno del prosciugamento (1656–1657) e la vita di Noè fino al 2006 — binario storico a 0 su 18, composizione 18 su 18. Nessun errore in console.
    *Aperto:* `ararat` resta `majority` con l'identificazione con Urartu **non** verificata su una fonte diretta (la nota geografica dice solo il dato testuale: «i monti di Ararat» è plurale costrutto, quindi una regione e non una cima).
  - [x] **Chiusura dei sei punti aperti dei blocchi Gen 4–5 e Gen 6–9 (22/07/2026).** Decisioni prese dall'utente, eseguite come modifica dati in `bootstrap/`.
    *1 e 4 — forbici di composizione allargate a -900/-400* su `gen-genealogia-adamo-noe` e sulle quattro pericopi del diluvio, con due voci distinte in `posizioni` (materiale sacerdotale / materiale non sacerdotale). Motivo: su Gen 5 l'attribuzione sacerdotale vale per la **cornice**, non per il materiale — Carr 2020 sostiene che P riprese un rotolo dei *toledot* di Adamo pre-sacerdotale, con la sua cronologia; sul diluvio i due materiali sono intrecciati nello stesso testo. Una forbice stretta asseriva più di quanto le fonti autorizzino, e la forbice è ciò che la timeline disegna: l'avvertenza testuale non la corregge. **Effetto visibile da mettere in conto:** tutte e dieci le pericopi dei due blocchi hanno ora la stessa forbice, quindi sul binario della composizione Gen 4–9 si legge come una fascia unica; è la conseguenza corretta della decisione, non un difetto di resa.
    *Decisione di schema consapevolmente rimandata:* il modello non ha una casella per «due datazioni di composizione nello stesso passo». Oggi si usa un `composizione.range` largo più due voci in `posizioni`. Un campo dedicato va progettato quando la curation avrà attraversato abbastanza casi da sapere che forma deve avere — non su un campione di quattro pericopi. `SCHEMI-DATI.md` **non** è stato modificato.
    *3 — le cifre a schermo sono di un testimone solo.* Nuova nota `gen5-tre-tradizioni-testuali`: TM, Settanta e Samaritano divergono sulle età dei patriarchi, e le divergenze sono revisioni deliberate, non errori di copia; secondo Carr quale testimone conservi la cronologia più antica **cambia fra Gen 5 e Gen 11,10-26**, quindi non ce n'è uno da preferire in blocco. La nota non riporta i numeri delle altre tradizioni: non sono nel dataset e non si ricostruiscono a memoria. Rimando di una riga nelle `nota` di Gen 5 e della pericope del diluvio, dove le cifre compaiono.
    *5 — `ararat`:* status `majority` confermato (l'equazione onomastica אררט = accadico *Urarṭu* è solida, l'estensione della regione no), fonte *Encyclopaedia Judaica* aggiunta al luogo e alla nota geografica, che ora riporta anche il disaccordo delle tradizioni antiche su quale sia la montagna (Ṭure Qardu nelle versioni aramaica e siriaca, monte Lubar nei Giubilei, monte Nimush nella tradizione babilonese). Nuova nota `gen84-urartu-terminus-post-quem`: *Urarṭu* subentra a *Uruaṭri* e *Nairi* nelle fonti assire all'inizio del I millennio, quindi la forma attuale del racconto non è anteriore — indizio linguistico indipendente dall'analisi delle fonti.
    *6 — «Adamo» nome proprio, conflitto di F2.3 sciolto.* La letterale rende ora «l'uomo» nelle tre occorrenze taggate `Npm` (gen.2.20.11, gen.3.17.01, gen.3.21.04); `gen.2.20.02` era già corretta. La divergenza col tagging è dichiarata e verificabile: il testo consonantico non decide, la vocalizzazione masoretica dice solo che l'articolo non c'è, e **il passo dall'assenza di articolo al nome proprio è una scelta lessicale di TAHOT**, non un dato. L'argomento decisivo è interno: in gen.2.20 lo stesso referente è taggato prima nome comune articolato e poi nome proprio, a nove parole di distanza. Note `gen220-adam-nome-proprio` e `gen320-nomi-propri-tardivi` riscritte in `bootstrap/notes-f23-revisione.json` (id già presenti in `public/data/notes.json`: da sostituire, non affiancare), `gen41-nomi-propri-da-qui` allineata.
    *2 — Gen 4,26 e il nome divino:* nessuna modifica, come deciso. L'argomento sulle fonti resta dichiarato non asserito e rinviato a F5.3.
    *File aggiunto non previsto dal handoff:* `bootstrap/translations/index.json`. Il validatore rifiuta una cartella `translations/` senza manifest, ed è lo stesso file che F2.3 aveva prodotto accanto alla bozza della letterale.
    *`da_verificare` resta `true` su tutti i record toccati*, anche quelli che ora hanno una fonte solida: F5.3 è una passata unica, e abbassare il flag in anticipo renderebbe ambiguo il significato di `false`.
    *Verifica:* `npm run valida` verde, 3 avvisi attesi di id presenti sia in bozza sia nel file curato (`nod`, `ararat`, le due note di F2.3).
  - [ ] **Blocco Gen 10 — bozze pronte, in attesa di revisione (23/07/2026).** In `bootstrap/`: `events-gen10.json` (5 pericopi contigue su Gen 10,1-32: Iafetiti 1-5, figli di Cam e di Cus 6-7, Nimrod e i primi regni 8-12, discendenti di Egitto e Canaan 13-20, figli di Sem 21-32), `notes-gen10.json` (6 note: 4 storico-critiche/filologiche, 2 `tradizione_ebraica`), `places-gen10.json` (il solo `rehoboth-ir`), più 10 glosse aggiunte a `bootstrap/lexicon_it.json` (269 in tutto).
    *Struttura decisa con l'utente:* Nimrod (10,8-12) è pericope a sé — è l'unico brano narrativo del capitolo, dove stanno Babele, l'Assiria e le note più corpose. Le persone (86 eponimi già in `people.json`) e i luoghi (già in `places.json` da TIPNR/OpenBible) **non** hanno richiesto bozze: le liste per pericope sono estratte dal disco dai `riferimenti`, non a memoria.
    *Fonti verificate in sessione (lette dal browser, il dominio dà 403 a WebFetch):* Petersen, *The Primeval History* (Gen 10 fra le genealogie della storia primordiale); HarperCollins Bible Dictionary, voci *Nimrod* e *Nineveh*; Sherman, *The Reception History of the Tower of Babel* — tutte Bible Odyssey (SBL). Per la tradizione ebraica, Rashi su Gen 10,9 (il «cacciatore davanti al Signore» come ribelle) e su Gen 10,25 (Peleg e la «divisione» di Babele; Ioktan «il piccolo»), dall'API Sefaria, Rosenbaum-Silbermann, Public Domain — sintetizzati in italiano e linkati, senza copiare testo nel dataset.
    *Tempo:* `tempo_narrato.am` è `null` su tutte le pericopi — Gen 10 è genealogia segmentata senza cifre, quindi il binario narrato resta vuoto; l'Anno Mundi riprende in Gen 11,10. Il binario storico resta a zero (storia primordiale, Petersen).
    *tempo_storico rivisto in revisione (23/07/2026):* le prime bozze avevano nelle sintesi identificazioni geografiche dei popoli (Iafet = nord/Egeo/Anatolia; Cam = Africa NE + costa siro-palestinese; ecc.) **non sostenute da Petersen**, l'unica fonte citata — rimosse (regola 2). Restano il quadro autorizzato dalla fonte e le code puramente testuali (elenco delle linee, confine di Canaan da 10,19, Nimrod e le città dal testo).
    *Composizione (rivista in revisione, 23/07/2026):* la prima bozza applicava a tutte e 5 le pericopi la forbice del materiale non sacerdotale (Nihan-Römer + Scullion), ma **la cornice genealogica della tavola dei popoli è classicamente sacerdotale** — le fonti della forbice non-P non le competono. Riformulata in un'unica posizione «Perché una forbice sola su un testo intrecciato» (cornice di stile sacerdotale + inserto non sacerdotale di Nimrod, attribuzione verso per verso **non asserita**, range -900/-400 che copre entrambi gli strati), **senza fonti**: Gen 10 entra così nel batch rinviato con le 4 pericopi del diluvio.
    *`rehoboth-ir`:* unico nome geografico di Gen 10 senza record (assente da OpenBible e TIPNR). Creato a mano su richiesta dell'utente: `status: disputed`, `candidati: []`, `tipnr_id: null`, con fonte che dichiara l'assenza di geolocalizzazione. Sito non identificato, da decidere in F5.3.
    *Lessico:* i **gentilizi** (Gebuseo, Amorreo, Cananeo, Caftorita…) e i nomi propri **non** entrano nel lessico (decisione dell'utente): la letterale li rende dai record di `people.json`/`places.json`. Restano 10 nomi comuni glossati (costa, nazione, lingua, selvaggina, regno, territorio, «verso», maggiore, sede, monte). **Anomalia di tagging segnalata:** `H1419K` in gen.10.21 (הגדול «il maggiore», riferito a Iafet) porta lemma `גַּל` e glossa `old` — reso «maggiore» sul senso contestuale, da verificare in F5.3.
    *Verifica:* `npm run valida` verde, 3 avvisi attesi (id `nod`, `ararat`, note di F2.3 presenti sia in bozza sia nei file curati). L'anteprima in iframe del blocco è il passo successivo della revisione.
  - *Aperto (F3.4 → F5.3), l'estremo alto -900:* **il -900 dei `composizione.range` non sacerdotali non è ricavabile dal testo approvato di Gen 2-3.** Lì le posizioni vive partono dal VI secolo (jahvista basso, D-Komposition di Blum, unità di Rendtorff, Römer/K. Schmid, Otto post-sacerdotale); l'unica che starebbe più in alto — il jahvista di età salomonica, X sec. — è quella che il manuale Römer-Macchi-Nihan dà per lasciata dalla maggioranza, e cade comunque fuori dal -900. Il range **copre** il ventaglio ma non vi **coincide**: per questo la formula condivisa delle sintesi usa il verbo «copre», scelto apposta e non da ripulire come imprecisione. Correggere l'estremo è un'altra decisione, non un ritocco redazionale: tocca 14 pericopi, **4 delle quali in `public/data/`**.
  - *Aperto (F3.4), le posizioni «forbice sola su un testo intrecciato» — ora 9:* le 4 pericopi del diluvio (`gen-corruzione-e-arca`, `gen-il-diluvio`, `gen-le-acque-si-ritirano`, `gen-patto-con-ogni-vivente`) e, dalla revisione del 23/07/2026, le **5 pericopi di Gen 10** (`gen-tavola-iafetiti`, `gen-figli-di-cam-e-cus`, `gen-nimrod-primi-regni`, `gen-camiti-egitto-e-canaan`, `gen-figli-di-sem`) hanno una posizione propria che spiega perché un solo `range` su un testo a fonti intrecciate — tutte con **0 fonti**. Due condizioni fissate per quando si deciderà: (a) se la posizione giustifica un **limite del modello dati**, va etichettata come scelta editoriale del progetto, sul precedente di `gen-primo-racconto` («scelta editoriale del progetto da confermare in revisione»), altrimenti una decisione sullo schema si presenta come posizione della letteratura; (b) le fonti sosterrebbero la **premessa** (per il diluvio: doppioni su animali e durata; per la tavola dei popoli: cornice genealogica di stile sacerdotale + inserto narrativo di Nimrod), non la scelta editoriale. Nelle sintesi la parola «semplificazione» ricorre senza quella qualifica.
  - *Aperto (tutto F3.4):* il binario degli ancoraggi storici resterà a zero anche a Gen 4–11 curati per intero — Gen 1–11 è storia primordiale e la scholarship mainstream non le riconosce ancoraggi diretti (specifica §3.1). Non è un buco da riempire con altra curation: quella riga è il dato.

## Fase 4 — Assistente RAG

- [ ] **F4.1 Embeddings.** `scripts/gen-embeddings.ts` via Ollama (BGE-M3), sorgente: letterale + note curate; `embeddings.json` con meta.
- [ ] **F4.2 Retrieval + generazione.** Cosine similarity client-side, top-k, chiamata a Ollama con il system prompt del §9 della specifica; rilevamento disponibilità Ollama e istruzioni `OLLAMA_ORIGINS` documentate.
- [ ] **F4.3 Guardrail.** Post-verifica dei riferimenti (esistenza nel dataset + presenza nel contesto recuperato; altrimenti blocco/warning), inserimento dei versetti dal database (mai dal modello), banner permanente, riferimenti cliccabili.
  *Fatto quando:* un riferimento inventato ad arte viene bloccato; i versetti citati compaiono sempre dal database.

## Fase 5 — Rifinitura

- [ ] **F5.1 Ricerca** (testo traduzioni, lemmi, entità).
- [ ] **F5.2 Export/backup del dataset** curato.
- [ ] **F5.3 Passata di verifica dei `da_verificare`.** ⏸ REVISIONE — rassegna in blocco di **tutti** i record curati rimasti con `da_verificare: true` (events, notes, places, people, lexicon): controllo delle fonti, conferma o correzione di confidenze e status, abbassamento del flag a `false` dove verificato. **Decisione dell'utente (21/07/2026):** durante le fasi di curation non ci si ferma sui `da_verificare`, si producono le bozze complete e si prosegue; la verifica avviene qui, una volta sola.
- [ ] **F5.4 Pulizia finale** e README tecnico (setup, comandi, come aggiungere una traduzione personale, come rigenerare i dati).
